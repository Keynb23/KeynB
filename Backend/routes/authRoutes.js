// routes/authRoutes.js
import express from 'express';

export default function authRoutes(db) {
    const router = express.Router();
    
    // Placeholder for future /api/auth/login and /api/auth/logout routes
    
    return router;
}